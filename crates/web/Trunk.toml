[build]
target = "index.html"
dist = "dist"
# Default asset prefix; override with `TRUNK_BUILD_PUBLIC_URL` or `--public-url` (see src/config.rs).
public_url = "/"

[[proxy]]
rewrite = "/api/v1/"
backend = "http://127.0.0.1:8000/api/v1"

[tools]
tailwindcss = "3.4.4"
