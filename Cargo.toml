[workspace]
members = [
    "crates/api",
    "crates/board",
    "crates/types",
]
# Examples are excluded because they depend on external crates not in this repo
# examples/axum-email-service and examples/actix-ntfy-service
resolver = "2"
